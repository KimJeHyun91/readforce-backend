services:
  - type: web
    name: readforce-backend
    # env: 또는 runtime: 줄이 여기에 없어야 합니다.
    plan: free
    buildCommand: "./gradlew build -x test"
    startCommand: "java -jar build/libs/readforce-0.0.1-SNAPSHOT.jar"
    envVars:
      - key: SERVER_PORT
        value: 10000